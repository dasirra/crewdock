> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

# Plan: Add SQLite Tracking Database to Forge (Issue #1)

**Date:** 2025-07-16  
**Issue:** #1 — Forge: add SQLite tracking database  
**Branch:** `1-sqlite-tracking`

## Goal

Replace Forge's fragile dedup logic (branch name parsing + session label matching) with a persistent SQLite database that tracks all issues across sessions.

## Tasks

### Task 1 — Add `sqlite3` to Dockerfile ✅
- Add `sqlite3` to the `apt-get install` list in `Dockerfile`

### Task 2 — Create `forge-db.sh` helper script ✅
- Location: `agents/forge/forge-db.sh`
- Commands: `init`, `check`, `queue`, `start`, `done`, `fail`, `skip`, `list`, `eligible`, `reset`, `stats`
- Database location: `$HOME/.openclaw/workspace/agents/forge/forge.db`
- Schema: `issues` table with status tracking, attempt counting, session_id, pr_number

### Task 3 — Update `agents/forge/SOUL.md` ✅
- Replace old issue selection section with SQLite-based flow
- Add new interactive commands: `retry`, `skip`, `history`, `stats`
- Add `maxAttempts` to config resolution table
- Add DB paths to the Paths section

### Task 4 — Update `agents/forge/AGENTS.md` ✅
- Add `forge-db.sh init` to cron cycle workflow
- Add `forge-db.sh start/done/fail` to session lifecycle
- Add new commands to interactive section
- Add SQLite tracking database section
- Add `maxAttempts` to config table

### Task 5 — Create `agents/forge/config.example.json` ✅
- Add `maxAttempts: 3` to defaults

### Task 6 — Create `setup.sh` ✅
- Copy `forge-db.sh` to workspace
- Run `forge-db.sh init`

## Files Changed

| File | Change |
|---|---|
| `Dockerfile` | Added `sqlite3` to apt install |
| `agents/forge/forge-db.sh` | New — SQLite helper script |
| `agents/forge/SOUL.md` | Updated issue selection + new commands |
| `agents/forge/AGENTS.md` | Updated workflow docs |
| `agents/forge/config.example.json` | New — with `maxAttempts` |
| `setup.sh` | New — bootstrap script |
| `docs/plans/2025-07-16-issue-1.md` | This plan |
